# harness_hub

Catalog of agent harnesses for the FlexNetOS meta workspace. `registry.json` is the single source
of truth; the **Rust-native** `scripts/validate.sh` (the `hub-validate` crate) enforces it
(CI-enforced). No Python tooling — the workspace is Rust-native.

## Packaged harnesses (the `harness` plugin)

The vendored `harness/` plugin is a **factory + a library of packaged harnesses**. `/harness:harness`
builds harnesses; `/harness:<name>` runs a ready-made one. Every new harness follows the
[packaged-harness standard](docs/packaged-harness-standard.md): an orchestrator skill in
`harness/skills/<name>/`, agents in the shared `harness/agents/` pool (infra shared, specialists
name-prefixed), a `registry.json` catalog row, and ejectability into a target repo's `.claude/`.

**Harness: meta-plugin (`/harness:meta-plugin`)** — the pilot packaged harness. An autonomous,
resumable, self-restarting Ralph loop that organizes the meta workspace and runs the combined
maintenance processes (hub/registry consistency, cross-repo build/test/lint health, protocol↔consumer
drift, catalog/inventory organization), coordinated via harness_hub, meta-plugins,
meta_plugin_protocol, meta_plugin_api, over all repos in `.meta.yaml`.

**Trigger:** for repo-organization, catalog-sync, cross-repo-health, protocol-drift, workspace-audit,
or "resume/continue/re-run the loop" tasks, use `/harness:meta-plugin` (or `/meta-plugin` once
ejected). Simple one-off questions may be answered directly. To add a *new* packaged harness, use
`/harness:harness` and follow the packaged-harness standard.

**Harness: rust-port (`/harness:rust-port`)** — a full-feature, **no-downgrade** Rust-port loop. A
parity ledger inventories every source unit; each is ported fully (no stubs) then differentially
parity-verified (source vs Rust) before it counts as done; `DONE` only at 100% + a left-behind sweep.
**Trigger:** "port \<project\> to Rust", "rust port", "full-parity port", "resume the port".

**Harness: planning-engineer (`/harness:planning-engineer` · `/harness:plan-loop`)** — a strictly-gated,
read-only planning/architecture Ralph loop that turns a target (crate/subsystem/feature-area) into a
decision-grade plan (code graph + 90-day trend research → adversarially-verified gaps/upgrades →
ASCII-diagram plan + tool-eval, promoted to docs/ROADMAP + draft ADR). It **self-evaluates AND
self-upgrades the harness after every cycle** via the `evolution-steward` + `harness-evolution` method
(evaluate → mine lessons → queue/apply fail-closed; only ever strengthen a gate; at the cycle boundary,
never mid-cycle). **Trigger:** "plan \<subsystem>", "architecture plan", "plan the backlog", "resume the
planning loop". North-star spec: `prompt_hub/prompts/planning-engineer-loop.prompt.yml` (P9).

**Continuity:** committed `.handoff/loop/HANDOFF.md` is the authoritative cold-resume signal; weave is
only an observable heartbeat. The external runner
(`harness/skills/meta-plugin/scripts/ralph-meta-plugin.sh`) is **SAFE by default** — it does not
bypass the permission system; unattended apply is a deliberate, settings-authorized opt-in.

**Change history:**
| Date | Change | Target | Reason |
|------|--------|--------|--------|
| 2026-06-13 | Initial build: repo-org-loop harness — 5 agents, 5 skills, external SAFE runner | `.claude/` (repo-local) | Requested: loop workflow automating organization + combined processes via the 4 coordination repos |
| 2026-06-13 | Port catalog validator Python→Rust (`hub-validate` crate + `validate.sh`); rewire CI/README/schema; delete `validate.py` | `scripts/`, CI, README, schema | Owner constraint: Rust-native only, no Python |
| 2026-06-13 | Promote repo-org-loop into the `harness` plugin as packaged harness `/harness:meta-plugin`; shared agent pool; ejectable; catalog row; plugin v1.2.0→1.3.0; establish `docs/packaged-harness-standard.md` | `harness/agents/`, `harness/skills/`, `registry.json`, `entries/`, `docs/` | Owner vision: harness_hub = library of per-use-case packaged harnesses exposed as `/harness:<name>` |
| 2026-06-13 | Rust-native `register` subcommand + `scripts/register.sh` (inverse of eject); cataloged `handoff` + `weave` as peers | `scripts/`, `registry.json`, `entries/` | Repeatable one-command harness registration |
| 2026-06-13 | New packaged harness `/harness:rust-port` (full-feature no-downgrade Rust port loop) — 4 specialist agents + 3 skills + parity ledger; plugin v1.3.0→1.4.0 | `harness/agents/rust-port-*`, `harness/skills/rust-port*`, `registry.json`, `entries/` | Owner: automated full-parity Rust port harness (flagship: meta/Archon TS→Rust) |
| 2026-06-13 | Mandatory shared self-evolution: `evolution-steward` agent + `harness-evolution` skill + `LESSONS.md` ledger + a final **Phase E** wired into every harness (meta-plugin, rust-port) and the standard | `harness/agents/evolution-steward.md`, `harness/skills/harness-evolution/`, `harness/LESSONS.md`, both orchestrators + eject scripts, `docs/` | Owner: a common evaluation+upgrade agent that learns lessons each run and upgrades the harness |
| 2026-06-13 | rust-port upgrade: full wrap-up/handoff — new shared `session-relay-wrap-up` + `session-relay-resume` skills (ICM store/recall + weave inbox + verify-on-resume, ideas from weave session-relay & ICM hooks); wired into rust-port Phase 0/3 + eject | `harness/skills/session-relay-{wrap-up,resume}/`, `harness/skills/rust-port/`, `docs/` | Owner-directed via `/harness:harness-evolution`; meta-plugin may adopt (proposed, scope law) |
| 2026-06-13 | meta-plugin adopts `session-relay-wrap-up` + `session-relay-resume` (Phase 0/3 + eject + references) — continuity now uniform across both packaged harnesses | `harness/skills/meta-plugin/` | Owner approved the cross-harness proposal ("do it") |
| 2026-06-13 | New packaged harness `/harness:code-research` (deep, read-only code research & analysis — fan-out analysts → adversarial claim verification vs source → decision-grade report) — 4 specialist agents + 3 skills; plugin v1.4.0→1.5.0 | `harness/agents/code-research-*`, `harness/skills/code-research*`, `registry.json`, `entries/`, `docs/` | Owner: evidence-based analysis to settle "is Archon a harness/agent manager?" + reusable capability |
| 2026-06-13 | ADR-0001: `harness-agent-rs` — Rust harness/agent-manager runtime, ported from Archon's design + mapped onto hf/weave/grit/icm; markdown builder kept as-is; current-architecture-only (3 legacy versions excluded) | `docs/adr/0001-harness-agent-rs.md` | code-research verdict accepted; owner: "Archon is an agent harness manager — start the rust port to new repo harness-agent-rs" |
| 2026-06-13 | New shared skill `harness-loop-init` (idempotently lays down `.handoff/loop/` — a loop's first step) + init script; added to the 3 loop-harness eject lists + standard; staged the session handoff (`.handoff/loop/HANDOFF.md`: STEP1 code-research oh-my-pi → STEP2 rust-port DISCOVER) | `harness/skills/harness-loop-init/`, eject scripts, `docs/`, `.handoff/loop/HANDOFF.md` | Owner: build harness-loop-init + session handoff via /session-relay-wrap-up |
| 2026-06-13 | Kernel-backed loop flavor: `handoff-loop-init` (drives `hf init` for the FULL `.handoff` buildout + ledger-residency guard; kernel-first, fail-closed) + `handoff-loop` (witnessed `hf` loop: resume→drift→claim→checkpoint→policy→handoff); standard documents file-based vs kernel-backed; plugin v1.5.0→1.6.0 | `harness/skills/handoff-loop-init/`, `harness/skills/handoff-loop/`, `docs/` | Owner: "/handoff-loop-init build the .handoff kernel and full .handoff buildout" (was not registered) |
| 2026-06-13 | Vendored the `handoff-loop` harness from `meta/handoff` into `harness_hub/handoff-loop/` (.claude [12 skills+9 agents], .agent, .handoff [minus ledger.db], .grit, .github, docs, schemas, scripts, guide files); EXCLUDED the Rust kernel crates (hf/ledger/work-order) + build/scratch. Renamed plugin skill `handoff-loop`→`handoff-loop-run` (do-not-delete); anchored `.gitignore` `/.claude/*` so the vendor's `.claude` is fully tracked | `handoff-loop/`, `harness/skills/handoff-loop-run/`, `.gitignore`, `docs/` | Owner: "create handoff-loop folder, copy .claude + .agent from meta/handoff; change the name, don't delete" |
| 2026-06-13 | Continuous-autonomous-cadence rule (merged from master #15): a loop runs plan→implement→test→next continuously to a ~50% context budget, not stop-and-ask per item; only genuine walls halt | `docs/packaged-harness-standard.md`, `harness/LESSONS.md`, `.handoff/loop/{evaluation,proposed-upgrades}.md` | Concurrent meta-session rule reconciled into develop during the develop↔master merge |
| 2026-06-13 | Eject now prints a **`SessionStart` recall-hook** snippet (`.claude/settings.json`): deterministic pre-session ICM priming (`icm recall-context` at every session start, no model decision, graceful no-op if ICM absent) — the **most important** memory layer (a missed recall makes the whole session run blind; outranks an end-of-session store, per owner). Travels with the harness so an instance ejected OUTSIDE the meta workspace (which wouldn't inherit the user-global SessionStart hook) still gets the priming; the `icm-memory` skill is the as-needed mid-task complement. plugin v1.10.0→1.10.1 | `harness/skills/rust-port/scripts/eject.sh`, `harness/skills/rust-port/references/eject.md`, `harness/skills/icm-memory/SKILL.md`, `harness/.claude-plugin/{plugin,marketplace}.json` | Owner: "pre session hooks are more important than the stop hooks" → carry the deterministic recall-priming with the ejected harness |
| 2026-06-13 | New shared skill **`icm-memory`** + wired into rust-port: agents get a skill to **recall/store ICM durable memory AS NEEDED** (recall before non-trivial work; store on triggers — decision/error-resolved/preference/significant-task) — *runtime-delegated by the lead, NOT forced per-agent hooks* (owner: per-agent hooks reduce flexibility; "the agents must have a skill to call and query icm as needed… makes this harness flexible for use anywhere"). Graceful no-op when ICM is absent (portable to any repo). Added to rust-port's available skills + eject list + the packaged-harness-standard shared-skill set; complements the loop-boundary session-relay ICM store/recall with finer in-cycle agent memory. plugin v1.9.0→1.10.0 | `harness/skills/icm-memory/SKILL.md`, `harness/skills/rust-port/{SKILL.md,scripts/eject.sh,references/eject.md}`, `entries/rust-port.md`, `registry.json`, `docs/packaged-harness-standard.md`, `harness/.claude-plugin/{plugin,marketplace}.json` | Owner: agents must be able to query/store ICM as needed — a callable skill, not hard-wired hooks |
| 2026-06-13 | rust-port MERGE hardening ("tune it / find gaps + upgrade", independent gap-hunt + adversarial verify): closed 9 merge gaps (4 HIGH), all rooted in "the harness forgot Y is a real, separate, consumer-having, mutable git repo". **(G1) Y git discipline** — all Y writes in a per-task worktree on a feature branch (`dest_branch`/`dest_worktree`/`dest_base`), commit-per-merge-cycle to Y, PR-into-Y + auto-merge at merge-DONE (two commits/cycle). **(G2) Dual no-downgrade** — capture Y's own behavioral baseline at DISCOVER + a Y-regression diff after each merge (`findings/y-regression.md`); a merge that regresses Y's behavior is `- [!]`/`- [≠]`, symmetric with the X rule. **(G3) Atomicity** — concrete rollback (`git reset --hard`/worktree-discard, release grit locks on rollback), commit-iff-all-gates-pass. **(G4) Up-front unit classification** (`port-fresh`/`extend-Y`/`reuse-Y`/`map-onto-substrate`) from the researcher reuse map drives ITERATE — reuse-Y/map-onto skip the fresh port (verify Y/substrate against X), no wasted re-ports. **(G5)** Y-drift rebase + re-verify drifted merged units on resume. **(G6)** Y-runnable + substrate-availability fail-fast at DISCOVER. **(G7)** breaking-contract *resolution* (additive/shim/version), not just flag. **(G8)** merge order by Y's dependency graph. **(G9)** dup-scan before new-module, symbol-grain via re-verify, lock-release-on-rollback. plugin v1.8.0→1.9.0 | `harness/skills/rust-port-merge/SKILL.md`, `harness/skills/rust-port/references/merge-ledger.md`, `harness/agents/rust-port-{merge-integrator,researcher,architect}.md`, `harness/skills/cross-repo-reference/SKILL.md`, `harness/skills/rust-port/{SKILL.md,scripts/loop_state.template.md}`, `entries/rust-port.md`, `harness/.claude-plugin/{plugin,marketplace}.json`, `harness/LESSONS.md` | Owner: "tune it. find gaps and upgrade the rust-port merge" — bidirectional no-downgrade + real Y git/lifecycle |
| 2026-06-13 | rust-port extension (via `/harness:harness`): **port-and-MERGE + research/cross-repo agents + automated 3-model workflow.** (1) MERGE arc (ADR-0001 `rust-port→rust-port-merge`): port repo X→Rust **and merge each verified unit into destination repo Y** — new `rust-port-merge-integrator` agent + `rust-port-merge` skill + `references/merge-ledger.md` (landing decision new/merge-into/map-onto-Y-substrate, grit symbol-locks, reuse>duplicate-never-by-narrowing, **re-parity-verified in Y's context**); ITERATE gains a MERGE step + DONE gains merge-ledger-100%/Y-green; `loop_state` gains `dest_repo` + `merge:` counter. (2) New `rust-port-researcher` (X⟷Y reuse map; reuses code-research+deep-research) + `rust-port-cross-repo-referencer` (blast-radius/contract map via git-kb/meta) + `cross-repo-reference` skill. (3) **3-model tiering** (owner-chosen gate-protected): opus on gates+hard design (architect, parity-verifier, merge-integrator, evolution-steward, cartographer pre-DONE sweep), sonnet on workers (cartographer-inventory, porter, researcher, continuity), haiku on mechanical (build-health, cross-repo-referencer) — **gates never tiered down; tiering safe because the opus gate catches worker downgrades**; shared agents overridden per-call (scope law). Team 7→10 agents; plugin v1.7.1→1.8.0 | `harness/agents/rust-port-{merge-integrator,researcher,cross-repo-referencer}.md`, `harness/agents/rust-port-{cartographer,porter}.md` (model), `harness/skills/{rust-port-merge,cross-repo-reference}/`, `harness/skills/rust-port/**`, `entries/rust-port.md`, `registry.json`, `harness/.claude-plugin/{plugin,marketplace}.json`, `harness/LESSONS.md` | Owner: "upgrade /harness:rust-port — add merge skills/agent/workflows (port X→Rust→merge into Y), 3-model workflow, advanced research + cross-repo agents" |
| 2026-06-13 | rust-port `/verify` fix + scale upgrades: **(bug)** the symbol-map harvest prescribed `git kb index` but git-kb's command is **`git kb code index`** (verified by running git-kb 0.2.10: wrong form errors → empty harvest → the fail-closed rule would wall every port at DISCOVER); fixed all 6 occurrences (5 rust-port + 1 code-research-map). **(accuracy)** git-kb's `visibility` field is null (Rust) → derive the visibility filter from the `signature`'s pub/export marker. **(scale, for ports ≥ Archon)** symbol-map sharding by package (bounded per-cycle reads) + a DISCOVER **source-runnable baseline** (fail-fast NEEDS-HUMAN if the source toolchain can't run, vs every-unit INCONCLUSIVE). plugin v1.7.0→1.7.1 | `harness/skills/rust-port/references/symbol-map.md`, `harness/skills/rust-port/SKILL.md`, `harness/agents/rust-port-cartographer.md`, `harness/skills/rust-port-inventory/`, `harness/skills/rust-port/references/eject.md`, `harness/skills/code-research-map/`, `harness/.claude-plugin/{plugin,marketplace}.json`, `harness/LESSONS.md` | `/verify` ran the prescribed commands + a gap hunt for repos ≥ the flagship target; runtime-found bug + scale/runnability gaps closed (no-downgrade) |
| 2026-06-14 | rust-port ITERATE-gap upgrades (via `/harness:harness-evolution find` on MiroFish→teri ITERATE cycles 1–2; strengthen-only, scope law): **(F-G1 false-green baseline — highest value, gate strengthening)** the DISCOVER build-health gate had reported "142 tests GREEN @ c894de8" without executing it — c894de8 was a bad PR-#4 merge (duplicated `api_key`, dead `from_env` block) that does not compile, so the loop's whole no-downgrade reference was a phantom until the cycle-1 porter couldn't build (one iteration late). `build-health-auditor` now requires an **EXECUTED** `cargo build`+`cargo test` with the **real counts pasted** as evidence, and a **non-compiling dest tip is FAIL-CLOSED** (`NEEDS-HUMAN`/recorded repair) — never a GREEN/green-to-start verdict it didn't run; SKILL Phase 1 step 6 made the same executed-evidence + red-tip-fail-closed requirement explicit. STRICTLY STRENGTHENS the no-downgrade baseline gate. **(F-G2 cross-loop relay-noise hijack, recurrence-2)** a concurrent envctl/forge-loop `relay:resumed`/DriftSummary heartbeat reached the cycle-2 porter mid-run and it returned an ACK of that foreign message instead of its work-summary; added a rule to the porter def + the orchestrator agent-spawn contract — ignore relay/weave/notification traffic from other loops, ALWAYS return your own task's work-summary, never a relay-ACK (orchestrator treats a relay-ACK return as a missing summary and verifies via git). **(F-G3 port-in-place worktree build, PROPOSED not applied)** target==dest meta-member needs a worktree-only `[workspace]` build-aid (strip before develop→main) — promotion-adjacent, so proposed in `.handoff/loop/proposed-upgrades.md` for owner sign-off. | `harness/agents/build-health-auditor.md`, `harness/agents/rust-port-porter.md`, `harness/skills/rust-port/SKILL.md`, `harness/LESSONS.md`, `.handoff/loop/{evaluation,proposed-upgrades}.md` | `/harness:harness-evolution find` (HAND OFF) on ITERATE cycles 1–2: find gaps + strengthen the no-downgrade baseline/parity gates of the rust-port-merge (strengthen-only) |
| 2026-06-22 | rust-port tail-discipline upgrades (via `/harness:harness-evolution` retro on network-control rust-port-merge **batch-3 cycles 1–5**, #72–#76; strengthen/clarify-only, scope law): **(L1 confirmable-tail)** "no more primary-kind units (device/source WRITE exhausted/carved/owner-walled)" is the *tail*, not DONE and not NEEDS-HUMAN — SKILL Phase 2 step 2 now enumerates the adjacent-plane confirmable tranche (GUI-parity-over-engine, host/runtime-plane verify, legacy deprecation, READ-half of carved writes) before any terminal verdict. **(L2 carve READ/WRITE-split)** parity-ledger carve discipline: split the confirmable+non-destructive READ half from the owner-sensitive WRITE half, and never assert a leaf "not locatable" without exhausting the port's proven discovery method (evidence: TASK-0053b READ shipped #76 from the same configJson method the carve called "not located"). **(L3 honest-verification-under-absent-state)** when the host lacks the STATE to live-exercise a path, the honest PASS = tested parser over the real kernel/on-disk format (fixtures of each real state) + live-smoke of the absence path + documented gap (evidence: TASK-0026 bond-LACP #73). **(L4 deprecation≠deletion)** rust-port-merge git-hygiene: delete a legacy unit only if ported-and-orphaned (every capability `- [x]` + zero live callers); freeze-in-place the un-ported-and-coupled (evidence: TASK-0060 #75 froze `homelab_control.py`+`config/distributed.py`, removed `omada_api.py`). No gate strength changed; no network-control gate touched. | `harness/skills/rust-port/SKILL.md`, `harness/skills/rust-port/references/parity-ledger.md`, `harness/skills/rust-port-merge/SKILL.md`, `harness/LESSONS.md`, `network-control/.handoff/loop/{evaluation,proposed-upgrades}.md` | Mid-batch retro on a clean tail batch: encode the four tail-discipline lessons the loop handled by operator judgment (strengthen/clarify-only) |
| 2026-06-14 | rust-port `[≠]`-bar tightening (via `/harness:harness-evolution find` on MiroFish→teri ITERATE cycles 8–9; MATERIAL owner-flagged; strengthen-never-weaken, scope law): owner flagged that `- [≠]` "noted as optional sounds like a feature skip — a bad-behavior flag." `- [≠]` intentional-divergence was being used to SKIP **portable features** by rationalizing "the dest's architecture won't use it", and the parity gate ACCEPTED it: **U-018** `to_reddit_format`/`to_twitter_format`/`to_dict` (`[≠]` "teri consumes SocialProfile directly, OASIS export not needed") and **U-004** rotating-file logging (`[≠]` "console-by-design") — both portable observable-output features (serialization shapes / a file sink), both corrected to PORTED (9aa1dd0, 1d8e293) + re-verified; the U-018 skip had ALSO hidden a second downgrade (bio+persona collapsed into one field) the serializers exposed. Defined the `[≠]` bar PRECISELY: legal ONLY when the source behavior is **(a)** genuinely INEXPRESSIBLE in the dest (→ really `- [!]`), **(b)** NON-CONTRACTUAL/unobservable (retry jitter; a filtered-before-recorded op; a GIL/console artifact), or **(c)** a strict SUPERSET (dest has MORE, loses nothing); **NEVER** as "won't be used"/"probably unused"/"consumes it directly so the export isn't needed"/"`serde` covers it" for a feature with a **distinct observable output** (serialization shape, export format, file sink, CLI flag, render path). **A portable feature is ported, not `[≠]`-skipped — when in doubt, PORT IT.** The **parity-verifier must CHALLENGE every `[≠]` and FAIL a disguised skip** (route back to porter). Legitimate `[≠]` sanity-checked still valid under the bar (retry jitter, REFRESH-in-`FILTERED_ACTIONS`, `is_supported` json-superset). Interim `[≠]` re-audit of this run's ~20+ existing `[≠]` rows QUEUED in proposed-upgrades.md. STRICTLY STRENGTHENS the no-downgrade gate (a "won't be used" `[≠]` that used to pass now FAILs); no gate weakened. | `harness/agents/rust-port-{parity-verifier,porter,architect}.md`, `harness/skills/rust-port-translate/SKILL.md`, `harness/skills/rust-port/references/{parity-ledger,merge-ledger,runtime-constructs}.md`, `harness/LESSONS.md`, `.handoff/loop/{evaluation,proposed-upgrades}.md` | `/harness:harness-evolution find` on ITERATE cycles 8–9: owner-flagged `[≠]`-as-feature-skip → tighten the `[≠]` bar + make the gate challenge it (strengthen-only) |
| 2026-06-14 | rust-port DISCOVER-gap upgrades (via `/harness:harness-evolution find` on MiroFish→teri DISCOVER iter1; strengthen-only, scope law): **(F4 inventory accuracy)** default vendored-dependency exclude list (`.venv`/`site-packages`/`node_modules`/`dist`/`build`/`target`/`vendor`) + a documented per-language AST fallback (Python `ast`, TS/JS `tsc`/`<script>`, Rust `syn`) for when git-kb's indexer is unconfigured for a source tree — stops a bundled `.venv` (15k+ files) flooding the symbol denominator; empty-harvest fail-closed still applies. **(F1 drop-resilience)** agent-spawn contract: DISCOVER agents emitting large/multi-file deliverables (architect/cartographer/researcher) write each artifact incrementally + return a <400-word pointer-summary, never the full content (an architect socket-close at ~400s lost a whole phase with nothing on disk). **(F2 phase durability)** commit each DISCOVER deliverable as it lands + a `discover_progress:` checklist in loop_state + a RESUME-DISCOVER branch — an interrupted DISCOVER resumes at the first `pending`, not redone. **(F3 port-in-place)** `rust_target == dest_repo` config documented — merge collapses to "landed-in-dest + dest-not-regressed", merge-ledger tracks class+landing, parity gate doubles as the dest-regression gate, one commit/cycle. **(F5 gate precision, recurrence-2, gate-adjacent)** per-unit source-runnable classification (locally-differentiable MUST-run vs external-service verified-via-substrate) replaces the whole-source binary halt — every true NEEDS-HUMAN trigger preserved, only the false halt on map-onto-substrate paths removed; flagged in proposed-upgrades.md for owner awareness. Cross-harness promotion of F1/F2 PROPOSED (not applied). | `harness/skills/rust-port/{SKILL.md,scripts/loop_state.template.md,references/{symbol-map,merge-ledger}.md}`, `harness/skills/rust-port-inventory/SKILL.md`, `harness/agents/rust-port-{architect,cartographer,researcher}.md`, `harness/LESSONS.md` | `/harness:harness-evolution find` on the DISCOVER loop: find the gaps + expedite/accuracy-up a no-downgrade rust-port-merge (strengthen-only) |
| 2026-06-18 | rust-port architect-in-loop routing (via `/harness:harness-evolution` on MiroFish→teri U-024; owner-directed; additive, strengthen-only, scope law): the `rust-port-architect` agent EXISTED but the ITERATE loop only invoked it "if a unit looks structural" (optional, judgment-based) — so large `extend-Y`/structural/blocked units could go straight to the porter, which then **guessed** the decomposition, treated a stub as a hard blocker, or risked a reuse-by-narrowing downgrade. Made the architect route **MANDATORY** in Phase 2 step 4 for any unit that is `extend-Y`, needs a structural/landing decision, is `- [!]`-blocked on a stub/missing wiring, OR exceeds a size/complexity threshold (~300 LOC / ~8 symbols / any cross-module wiring): the architect FIRST records a per-unit design to `findings/<unit>-architecture.md` (reuse-vs-narrowing call, substrate-mapping/facade decision, **de-risking the perceived blocker by measuring blast radius**, pre-identified `- [≠]`/`- [!]` risk flags, **and an ordered sub-cycle decomposition** — each sub-cycle independently portable + parity-verifiable in one cycle), and the **porter executes one sub-cycle per cycle** against that recorded design (never invents the decomposition/landing/reuse call). Synced the agent-runtime table row + clarified the architect agent def role 4b (it OWNS per-unit decomposition, not just DISCOVER-time layout) + I/O protocol. Evidence: U-024 (2573-line `ReportAgent` `extend-Y` blocked on a stubbed `ZepToolsService`↔`KnowledgeGraph` wiring) — architect-first produced the reuse=coexist call, measured the stub as a zero-caller leaf (blast radius ~0), and a 9-sub-cycle a→i decomposition; sub-cycle (a) then ported + byte-parity-PASSed + committed cleanly. STRENGTHENS the design-decision gate (forces a recorded design where the loop could previously skip it); relaxes no parity/DONE/build-health check. | `harness/skills/rust-port/SKILL.md` (Phase 2 step 4 + agent-runtime table), `harness/agents/rust-port-architect.md` (role 4b + I/O protocol), `harness/LESSONS.md`, `.handoff/loop/evaluation.md` | Owner: "upgrade the harness to include an architect (design, reasoning, and decision) and leverage harness-evolution" |
| 2026-06-13 | rust-port harness self-upgrade (designed+adversarially-verified via `/harness:harness-evolution`, no-downgrade-only): **(1) detailed symbol mapping** — new `references/symbol-map.md` + `.handoff/loop/symbol-map.md` (one row per source symbol, deterministic `git kb code symbols --json --limit -1` harvest, visibility-filtered, **empty-harvest fail-closed**), the unit→symbol **rollup rule**, and a two-grain pre-DONE sweep (a dropped method/field/variant/route can no longer hide); **(2) agent-runtime porting** — new `references/runtime-constructs.md` (reimplement-vs-MAP-ONTO `hf`/`weave`/`grit`/`icm`/provider-CLI per ADR-0001, no behavior dropped) + a `rust-port-translate` agent-runtime idiom-map section + cartographer runtime/concurrency inventory dimension + streaming/concurrency/cancellation/backpressure differential scenarios in the verifier; **(3) agent runtime contract** — explicit per-agent execution table in the orchestrator (the spec `harness-agent-rs` consumes) + team-size 6→7 fix; **(4) gap-fill** — eject.md/entries/registry de-staled to match `eject.sh` (9 skills incl. `harness-loop-init`, 7 agents incl. `evolution-steward`), `loop_state` symbol counter, plugin v1.6.0→1.7.0 | `harness/skills/rust-port/**` (SKILL + references/{symbol-map,runtime-constructs,parity-ledger,eject} + scripts), `harness/skills/rust-port-{inventory,translate,parity}/`, `harness/agents/rust-port-*`, `entries/rust-port.md`, `registry.json`, `harness/.claude-plugin/{plugin,marketplace}.json`, `harness/LESSONS.md` | Owner-directed harness evolution: deepen the parity/DONE gate (strengthen-only) — detailed symbol mapping, agent runtime, fill all gaps, 100% Rust-native no-feature-left-behind |
| 2026-06-21 | rust-port e2e durability + drift-surfacing (via `/verify` end-to-end on a fresh temp repo; strengthen-only): **(1 continuity-durability)** the harness's truth — agents/skills + `.handoff/loop/*.md` ledgers — is committed-or-lost, and a DIR-FORM gitignore (`.claude/`/`.handoff/`) silently SWALLOWS it (git can't re-include past an excluded parent dir, so `!`-exceptions can't rescue it). `eject.sh` now DETECTS a dir-form ignore + runs `git check-ignore` on representative agent/ledger paths + warns with the contents-form fix / `git add -f` fallback; `eject.md` documents the durability contract + the distinction from the `hf` kernel's regenerable `.handoff/**/*.db`. **(2 set-e crash, pre-existing #46-48)** git-hygiene base auto-resolve (`git symbolic-ref origin/HEAD`) aborted the whole script (exit 128) on any remoteless/fresh repo under `set -euo pipefail` — fixed with `|| true`; `main` added to the base fallback. **(3 unmerged-upgrade drift)** git-hygiene `[3]` split into `[3a]` (open PR — in-flight) vs `[3b]` (NO PR — unmerged work that may be FORGOTTEN, the architect-in-loop gap), and rust-port Phase E now states an applied upgrade is not done until MERGED. e2e-verified on a fresh temp repo (eject → seed → commit-per-cycle → scripts run); 3 LESSONS rows; plugin v1.14.0→1.15.0 | `harness/skills/rust-port/scripts/{eject.sh,git-hygiene.sh}`, `harness/skills/rust-port/references/eject.md`, `harness/skills/rust-port/SKILL.md`, `harness/.claude-plugin/{plugin,marketplace}.json`, `harness/LESSONS.md` | Owner via `/verify`: end-to-end-test the rust-port-merge harness on a fresh temp repo; cover the dot-dir commit/gitignore issue + the unmerged-vital-upgrade issue |
| 2026-06-20 | rust-port-merge harness upgrade from the 33-resume MiroFish→teri lessons (via `/harness:harness` + harness-evolution; owner-directed; additive / **strengthen-only**, scope law; **upgrades only — no gate weakened**): **(1 Chinese→English)** new `references/localization.md` — translate user-facing source text to the dest's shipping language without downgrading behavior (PRESERVE contract strings a consumer parses / TRANSLATE user-facing text / TRANSLATE-WITH-REVERIFY for LLM prompts; semantic-grain parity = same-trigger + same-interpolation + meaning + source-recorded + not-a-contract-string); opt-in `localize:` directive; wired into `rust-port-architect` (role 3c) + `rust-port-parity-verifier` (localization mode) + rust-port SKILL Phase 0. **(2 Lua)** new **EMBED-LUA** port-and-map verb (`mlua`) in `references/runtime-constructs.md` for intrinsically-dynamic constructs (template/expression engines, runtime tool/plugin dispatch, rules-DSL, hot reload) where static Rust is genuinely lossy — upgrade-only constraints (sandboxed/deterministic, committed first-class artifact, errors→Rust enum, **still differentially parity-verified**, never a stub-by-Lua); wired into architect role 2 + dep table. **(3 git hygiene)** rust-port-merge SKILL §Git hygiene & lifecycle (bounded worktree, delete-merged-branch, one held/draft PR opened at merge-DONE, `fetch --prune` on resume) + new fail-safe `scripts/git-hygiene.sh` (audit-default, merged-only `branch -d`, dirty-tree refusal, protected branches). **(4 accuracy/speed)** new `scripts/ledger-recount.sh` (deterministic marker recount + token-safe `--find`) + symbol-map §Mechanical maintenance (token-anchored row edits, one-symbol-per-line, mega-line cap) + loop_state NEXT-pointer cap (history→`findings/next-history.md`) + dependency-unblock re-scan (a `- [!]` blocked on a now-terminal symbol is re-scanned on flip/resume) + verify-only batching (no-mutation `reuse-Y`/`map-onto` sweeps may batch per cycle, per-symbol verdicts intact). **(5 all/optional features)** parity-ledger `[≠]` bar: **optional≠skippable** (an optional / behind-a-flag feature is still a portable TO-PORT, never a free `[≠]`). 9 lessons appended to LESSONS.md; plugin v1.13.0→1.14.0 | `harness/skills/rust-port/references/{localization.md (new),runtime-constructs.md,parity-ledger.md,symbol-map.md}`, `harness/skills/rust-port/scripts/{ledger-recount.sh,git-hygiene.sh (new)}`, `harness/skills/rust-port/SKILL.md`, `harness/skills/rust-port-merge/SKILL.md`, `harness/agents/rust-port-{architect,parity-verifier}.md`, `harness/.claude-plugin/{plugin,marketplace}.json`, `harness/LESSONS.md` | Owner: 'Upgrade /harness:rust-port-merge from Lessons Learned — git worktree/branch/remote org+cleanup, Chinese→English translations, accuracy+speed, upgrades only, ALL features + ALL optional features used, leverage Lua where it's an upgrade over rust-native' |
| 2026-06-25 | code-research Phase-E retro upgrades (via `/harness:harness-evolution` on the prompt_hub "map the prompt-load flow" run, PR #176; additive / **strengthen-only**, scope law, **no gate weakened**): **(1 gate-quality)** call-graph EDGES are claims-to-verify, not facts — the index name-resolves call sites and mis-binds same-named symbols (trait vs inherent method); wired into `code-research-map` (Discipline), `code-research-analyze` (edge is a lead → cite the call site), `code-research-verify` (refute checklist: mis-resolved edge ⇒ REFUTED). **(2 friction)** `code-research-map` gains an explicit **step 0**: build + verify the code index (prove non-empty, record counts) before trusting any AST tool — an empty index silently reads as "no usage". **(3 calibration)** severity claims must carry a threat model (input/boundary/sink + neutralizers) at the analyst source; verify's severity counter-example = non-exploitability ⇒ QUALIFIED. 4 lessons appended to LESSONS.md (3 applied, 1 noted) | `harness/skills/code-research-map/SKILL.md`, `harness/skills/code-research-analyze/SKILL.md`, `harness/skills/code-research-verify/SKILL.md`, `harness/LESSONS.md` | Owner: Phase-E retro on the code-research run — the verify gate refuted a git-kb call-graph edge (symbol-resolution artifact) and downgraded 2 severity overclaims; encode both wins + the empty-index friction |
| 2026-06-26 | planning-engineer/plan-loop **self-evaluate + self-upgrade after EVERY cycle** (via `/harness:harness` against the north-star prompt `prompt_hub/prompts/planning-engineer-loop.prompt.yml` P9; "leverage /harness-evolution"; additive / **strengthen-only**, scope law, no gate weakened): the single-cycle `planning-engineer` skill (Phase 5) + the registry + the prompt already required per-cycle self-eval/self-upgrade, but the **loop orchestrator, the shared agent def, the method skill, and the contract gate were not aligned** — the loop only said "evolution-steward self-eval" and batched the actual retro/apply to the `wrap_every` boundary, the agent still read "at DONE or HAND OFF" / "never mid-cycle", and nothing gated it. Reconciled all four to a per-cycle cadence (a *run* = one completed cycle; evaluate → mine → route → **queue/apply fail-closed at the cycle boundary, never mid-cycle**; the batch boundary becomes a *consolidation* pass — dedupe lessons, roll up recurrence, flush deferred low-risk upgrades), and **locked it with a new contract assertion** in `test-plan-contract.sh` (planning-engineer Phase 5 + plan-loop per-cycle self-eval/self-upgrade + harness-evolution + never-weaken guard + evolution-steward every-cycle/never-mid-cycle; resolves siblings in both plugin and ejected layouts). All 3 plan self-tests + `validate.sh` green. | `harness/skills/plan-loop/SKILL.md`, `harness/agents/evolution-steward.md`, `harness/skills/harness-evolution/SKILL.md`, `harness/skills/planning-engineer/scripts/{ralph-plan.sh,tests/test-plan-contract.sh}`, `CLAUDE.md` | Owner: "upgrade the /harness:plan-engineering-loop using prompt_hub/.../planning-engineer-loop.prompt.yml — ensure the loop can self-evaluate and self-upgrade after every run, leverage /harness-evolution" |
| 2026-06-26 | planning-engineer **full prompt-parity pass** (via `/harness:harness` audit vs the north-star `prompt_hub/prompts/planning-engineer-loop.prompt.yml`; "ensure the loop has everything the prompt describes"; additive / **strengthen-only**, no gate weakened): closed the genuine ABSENT/PARTIAL gaps a subagent parity-audit found. **(LAW)** added the standing toolchain law to `planning-engineer/SKILL.md` (fail-closed · owner-walls · TDD-native · **bun-not-pnpm / shimmy+ruvllm-replace-ollama-but-don't-remove-until-proven / clang-llvm-21-load-bearing** / evidence-over-vibes). **(P5)** extended the `plan-analyst` UPGRADE row schema → 4th axis `governance+settings+config`, `target-surface`, `effort`, `risk-tier: APPLY\|PROPOSE\|REGENERATE`, `acceptance` (the falsifiable condition its P8 RED test encodes — **1:1 with the test**), `reversibility` (NORTH-STAR triad Integrity·Reversibility·Capability-Gain); mirrored in the `plan-synthesis` roadmap rubric. **(P4)** `plan-synthesis` now REQUIRES the **control-plane diagram** (governance flow hooks→policy/guard→claim→scope→handoff + settings→runtime / toolchain→build→CI / policy.toml→cadence) plus target-state + fleet-level diagrams, not just a prose governance section. **(P6)** named the standing fleet toolset in the tool-eval (rtk/hf/git kb/meta/MCP/skills/**bun**/shimmy-ruvllm). **(P8)** created the previously-**vaporware** `scripts/differential-drive.sh` (drives the REAL binary, diffs vs golden, **fail-closed on 0 cases**) + wired it + the `tests-ran>0` / `hf test` count-verify + cargo/nextest/bun runner names into `plan-test-strategy`/`plan-test-strategist`. **(P2)** added Hugging Face research (`plan-trend-research`) + cross-repo-reference edges (`plan-cartographer`). **(governance detail)** hooks.toml 14-event contract + PILOT.toml + `.cliff.toml`/qodana/`.looprc` + explicit cross-surface-drift detectors in `plan-governance-config-auditor`. **LOCKED** all of it with a new prompt-parity assertion block in `test-plan-contract.sh`. 3 plan self-tests + `validate.sh` green. | `harness/skills/planning-engineer/SKILL.md`, `harness/skills/plan-synthesis/SKILL.md`, `harness/skills/plan-trend-research/SKILL.md`, `harness/skills/plan-test-strategy/SKILL.md`, `harness/agents/plan-{analyst,cartographer,governance-config-auditor,test-strategist}.md`, `harness/skills/planning-engineer/scripts/{differential-drive.sh (new),tests/test-plan-contract.sh}`, `CLAUDE.md` | Owner: "upgrade the /harness:plan-engineering-loop using prompt_hub/.../planning-engineer-loop.prompt.yml — ensure the loop has everything the prompt describes" |
| 2026-06-26 | **back-port the 7 envctl-only planning axes into the canonical harness** (the eject had diverged ahead of source-of-truth; additive / **strengthen-only**, no existing skill/agent touched): the envctl runtime carried 7 planning dimensions the plugin lacked, so future ejects from the canonical would have *lost* them. Ported faithfully (the canonical is already meta/owner-flavored, so the incidental envctl/weave/codex mentions match the existing voice) — **7 skills** (`plan-filesystem-layout`, `plan-dependency-graph` [TDP target-DAG], `plan-prompt-architecture`, `plan-memory-vector-intelligence` [ICM], `plan-autoresearch-loop`, `plan-rules-policy-org` [A2A], `plan-distributed-compute` [Pi/ESP32/Lua]) + their **7 auditor agents** + **2 runtime helpers** (`plan-artifact-gate.sh` fail-closed artifact/DONE-drift gate; `plan-weave-dispatch.sh` 5-lane Opus/weave dispatcher — genericized the hardcoded `rusty-idd-north-star` lane → `target-north-star` and the orchestrator default, behavior intact). Registered all 14 in `eject.sh` `OWN_SKILLS`/`OWN_AGENTS` + added apply-instructions for the two repo-root scripts. 3 plan self-tests (incl. the eject test that now exercises the 14 new OWN entries) + script `bash -n` green. | `harness/skills/plan-{filesystem-layout,dependency-graph,prompt-architecture,memory-vector-intelligence,autoresearch-loop,rules-policy-org,distributed-compute}/` (new), `harness/agents/plan-*-auditor.md` (7 new), `harness/skills/planning-engineer/scripts/{plan-artifact-gate.sh,plan-weave-dispatch.sh}` (new), `harness/skills/planning-engineer/scripts/eject.sh`, `CLAUDE.md` | Owner: "the larger harness_hub ← envctl back-ports (7 envctl-only axes + plan-artifact-gate.sh) are a separate sync" |

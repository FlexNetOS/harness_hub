# Loop state — code-research
session_started: <UTC e.g. 2026-06-13T16:00:00Z>   # you supply it; the runtime can't read the clock
loop: code-research
branch: <branch>
worktree: <abs path>
research_question: <e.g. "Is meta/Archon a harness/agent manager?">
target_root: <abs path of the codebase under study, e.g. ~/Desktop/meta/Archon>
target_toolchain: <bun | node | cargo | python | ...>   # so the verifier can RUN behavioral claims
report_path: <where the final report goes, or .handoff/loop/reports/<slug>.md>
cycle_budget: 3
cycles_this_session: 0     # reset to 0 on RESUME
cycles_total: 0
ledger: dimensions 0/<total> verified
last_item: (none — mapping only)
status: MAP complete — dimensions seeded
last_update: <UTC>

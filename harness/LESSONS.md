# Harness lessons ledger

Durable, **append-only** memory of lessons mined from harness runs by the `evolution-steward`
(via the `harness-evolution` skill). Recurrence is the signal: a lesson `noted` once becomes an
`applied`/`proposed` upgrade on its second occurrence. Never truncate this file — the history is
the point. (When a harness is ejected into a target repo, its ledger lives at the repo root as
`LESSONS.md`.)

| Date | Harness | Lesson (generalized class) | Evidence | Recurrence | Routed to | Status |
|------|---------|----------------------------|----------|-----------:|-----------|--------|
| 2026-06-13 | (seed) | Ledger initialized. Evolution-steward closes every run with evaluate → mine → route → apply/propose → record. | — | 0 | — | noted |
| 2026-06-13 | rust-port | Continuity should persist *reasoning* (decisions/lessons) to ICM at wrap-up and recall it at resume — committed loop state alone loses the "why". Generalized into `session-relay-wrap-up`/`-resume` (ICM store/recall + weave inbox + fail-closed verify). | owner-directed via /harness:harness-evolution | 1 | skills (session-relay-wrap-up, -resume) + rust-port orchestrator | applied |
| 2026-06-13 | all (shared) | The ICM-integrated wrap-up/resume continuity proved general — propagated to meta-plugin too, so continuity is uniform across packaged harnesses. Recurrence confirms it belongs in the standard for every harness. | owner approved cross-harness adoption | 2 | meta-plugin orchestrator + standard | applied |
| 2026-06-13 | all (shared) | **Cadence class: a loop must not gate task transitions on owner questions.** When the backlog + vision are known, run plan→implement→test→next continuously to a **context budget (~50%)**, not stop-and-ask after each item. Genuine walls still stop (NEEDS-HUMAN); everything else advances. | owner-directed via /harness:harness-evolution (merged from master #15) | 1 | standard (Continuous autonomous cadence) + feedback memory `autonomous-cadence`; per-orchestrator budget mechanics PROPOSED | applied |
| 2026-06-13 | rust-port | **Gate-completeness class: unit/file-level parity can hide symbol- and runtime-level downgrades.** A unit can be `- [x]` while a source symbol (method/field/variant/route) was never matched, or a runtime contract (parallel layer, streaming, cancellation) silently collapsed. Counting units, not symbols+runtime-behaviors, lets a downgrade slip. Added a symbol-map coverage gate (one row/symbol, deterministic harvest, rollup rule, two-grain sweep) + runtime-construct idiom coverage & streaming/concurrency/cancellation differential scenarios so DONE requires every symbol mapped+verified and every runtime contract exercised. | owner-directed self-upgrade (designed + adversarially verified) | 1 | rust-port DONE gate + `symbol-map.md`/`runtime-constructs.md` refs + cartographer/porter/verifier defs + `loop_state` symbol counter | applied |
| 2026-06-13 | rust-port | **Adversarial-verify caught a strengthen-that-could-weaken: a new symbol denominator harvested from an AST index is `0` for an un-indexable/un-indexed source → a vacuous `0/0` "clean" symbol sweep would pass DONE with nothing mapped — a hole the old unit-only gate (no AST dependency) didn't have.** Rule: a zero/empty symbol harvest of a non-empty source is fail-closed (`NEEDS-HUMAN`), never "no symbols"; map+denominator share one visibility filter so the diff stays sound/achievable. General lesson: when a new gate adds a computed denominator, make its empty/degenerate case fail-closed, not vacuously-pass. | gate-integrity verifier (workflow) | 1 | `symbol-map.md` empty-harvest rule + cartographer sweep + DONE gate | applied |
| 2026-06-13 | rust-port | **Runtime-contract class: a harness's own agent runtime was implicit prose (model/concurrency/retry/I-O scattered), so it was neither machine-executable nor a checkable gate.** Consolidated into one declarative per-agent Agent-runtime table in the orchestrator — the contract the future `harness-agent-rs` runtime (ADR-0001) consumes — making the gate-role of each agent (verifier=no-downgrade gate, cartographer sweep=DONE precondition, evolution-steward=strengthen-only) explicit and schedulable. | owner-directed self-upgrade | 1 | rust-port orchestrator SKILL (Agent runtime section) | applied |
| 2026-06-13 | all (shared) | **Catalog-drift class (recurrence): a harness's install truth is its `eject.sh` arrays; docs that enumerate skills/agents drift below them.** `eject.sh` shipped 9 skills/7 agents while `eject.md` prose, `entries/rust-port.md`, the orchestrator team-size, and SKILL's Skills line listed fewer (missing `harness-loop-init`/`evolution-steward`). Re-synced all to `eject.sh`. Same class as the prior session-relay/eject-list drift — a checklist "after editing eject.sh, sweep eject.md+entries+SKILL counts" belongs in the standard. | consistency audit | 2 | eject.md, entries/rust-port.md, rust-port SKILL.md; standard checklist PROPOSED | applied |
| 2026-06-13 | rust-port | **Unverified-command class: a skill prescribed `git kb index` (which doesn't exist — the subcommand is `git kb code index`), shipped because the design was reasoned, not RUN.** A `/verify` that actually ran the commands caught it: the wrong form errors → empty harvest → the same PR's fail-closed rule would wall EVERY port at DISCOVER. Lesson: every external CLI invocation a skill prescribes must be runtime-verified (run it, capture output) before ship — design review can't see a non-existent subcommand. Fixed all 6 occurrences (5 rust-port + 1 code-research-map). | `/verify` run (git-kb 0.2.10) | 1 | symbol-map.md/cartographer/inventory/eject.md + code-research-map; "runtime-verify prescribed commands" checklist PROPOSED for the standard | applied |
| 2026-06-13 | rust-port | **Model-tiering class: an all-opus harness can tier worker agents down (cost/speed) WITHOUT a quality downgrade — IF every no-downgrade GATE stays opus.** The safety argument: a sonnet worker that drops a branch is caught + FAILed by the opus parity gate, so tiering workers is a cost lever, never a correctness risk. Generalizes: any harness can introduce model-tiering by classifying agents into gate (never tier) vs worker (tier freely, with escalation). Applied as rust-port's gate-protected 3-model workflow (opus gates / sonnet workers / haiku mechanical); shared agents overridden per-call (scope law), not by editing shared frontmatter. | owner-directed (3-model workflow) | 1 | rust-port orchestrator (model tiering law + Agent-runtime table) + cartographer/porter frontmatter | applied |
| 2026-06-13 | all (shared) | **Capability-as-skill class: give agents a CALLABLE skill, not hard-wired per-agent hooks.** When the need is "agents should use ICM memory," the flexible answer is a shared `icm-memory` skill any agent invokes *as needed* (lead delegates at runtime) — NOT forced recall/store steps baked into each agent def. Hard-wiring reduces agent ability and portability; a skill keeps the harness usable anywhere (graceful no-op where the backend is absent). General lesson: prefer exposing a capability as a runtime-delegated skill over prescribing it as a fixed step in every agent. | owner correction ("you are trying to reduce the agent ability; the lead model will delegate at runtime") | 1 | new shared `icm-memory` skill + standard shared-skill set | applied |
| 2026-06-13 | rust-port | **Cross-repo-write class: when a harness writes into a SECOND repo Y, every single-repo assumption breaks — git flow, atomicity, the dual no-downgrade, and "is it already there".** A gap-hunt found 9 merge gaps (4 HIGH) all rooted in "the harness forgot Y is a real, separate, consumer-having, mutable git repo": no Y branch/worktree/PR (writes would land on Y's main), "rollback" was a ledger-mark not a tree-revert, the gate only protected X's behavior not Y's own, and the loop re-ported what Y already had (classification produced but never consumed). General lesson: any cross-repo-write harness needs (a) worktree+branch+PR per target repo, (b) atomic commit-iff-pass with concrete tree rollback, (c) a BIDIRECTIONAL no-downgrade gate (don't regress the target either), (d) up-front reuse-classification driving the loop, (e) target-drift reconciliation. | independent gap-hunt + adversarial verify of the just-shipped merge | 1 | rust-port-merge/merge-ledger + merge-integrator/researcher/architect + cross-repo-reference + orchestrator phases + loop_state | applied |
| 2026-06-13 | rust-port | **Merge class: a port isn't done at the port — landing it in a destination repo Y can itself downgrade (dropped re-export, narrowed type, collapsed streaming, reuse-by-narrowing onto a near-fit Y symbol).** So a merge needs its own ledger + a RE-verification of parity in Y's context (a standalone port PASS is necessary, not sufficient), reuse>duplicate but never reuse-by-narrowing, and symbol-locked parallel-safety. Added the rust-port→rust-port-merge arc (merge-integrator + rust-port-merge skill + merge-ledger + research/cross-repo-ref agents). | owner-directed (port-and-merge into Y) | 1 | merge-integrator/researcher/cross-repo-referencer agents + rust-port-merge/cross-repo-reference skills + merge-ledger.md + orchestrator MERGE phase | applied |
| 2026-06-13 | rust-port | **Scale/runnability class for ports ≥ the flagship target: (a) a flat symbol-map is too heavy at thousands of symbols → shard by package, bounded per-cycle reads; (b) the differential verifier MUST run the source, so a DISCOVER source-runnable baseline must fail-fast (NEEDS-HUMAN) instead of every-unit-INCONCLUSIVE.** Both surfaced answering "can the harness port a repo ≥ Archon". Added sharding guidance + a DISCOVER source-runnable precondition; neither relaxes a gate. | capability-question gap hunt | 1 | symbol-map.md (sharding) + rust-port SKILL Phase 1 (source-runnable baseline) | applied |
| 2026-06-14 | rust-port | **Inventory-accuracy class (over-coverage, the inverse of left-behind): a source tree often bundles its installed dependencies (`.venv`/`site-packages`/`node_modules`/`dist`/`target`/`vendor`), and a naive symbol harvest inventories 15k+ library-internal files as "source," flooding the denominator and making coverage both heavy and inaccurate.** Plus: git-kb's indexer may be *unconfigured for a source tree* (observed: Python source → empty harvest), so a deterministic per-language AST fallback (Python `ast`, TS/JS `tsc`/`<script>` parse, Rust `syn`) must be a documented default, not ad-hoc operator judgment. Both were manual operator scoping this run. Added a default vendored-dependency exclude list + per-language AST fallback to inventory/symbol-map/cartographer; empty-harvest fail-closed still applies after fallback (strengthens coverage accuracy, no gate relaxed). | DISCOVER artifacts: symbol-map.md l5-11 ("`git kb code index` not used"; "venv/node_modules/dist EXCLUDED") | 1 | rust-port-inventory SKILL + references/symbol-map.md + rust-port-cartographer | applied |
| 2026-06-14 | all (shared) | **Agent drop-resilience class: an agent producing a large/multi-file deliverable that buffers it all to the end loses the WHOLE phase on a mid-stream connection drop.** The architect died at ~400s/29 tool-uses (socket close) having written NOTHING to disk; the re-spawn that wrote each artifact incrementally and returned a short pointer-summary succeeded. General rule: any agent emitting a large/multi-file artifact MUST write each artifact/section to disk incrementally as produced and return only a short (<400-word) pointer-summary, never the full content — the on-disk artifact is the deliverable and a large return payload is itself a drop risk. Applied to rust-port DISCOVER agents (architect/cartographer/researcher) + the orchestrator agent-spawn contract; PROPOSED for the packaged-harness standard (cross-harness, scope law). | observed architect socket-close + successful incremental re-spawn | 1 | rust-port-architect/-cartographer/-researcher + rust-port SKILL (agent-spawn contract); standard PROPOSED | applied |
| 2026-06-14 | rust-port | **Phase-durability class: a long multi-step phase that commits only ONCE at its end strands every partial deliverable on any interruption — un-resumable via the documented RESUME path.** A prior DISCOVER left ledger/research/baseline on disk but no loop_state/symbol-map/architecture/HANDOFF (single end-of-DISCOVER commit), so it was redone. Fix: commit each DISCOVER deliverable as it lands + a `discover_progress:` checklist in loop_state so a dropped DISCOVER resumes at the first `pending` item. Generalizes: any long multi-agent phase should checkpoint+commit incrementally (PROPOSED for the standard, cross-harness). The DONE completeness sweep is unchanged — partial progress is durable, never reads as complete. | worktree git log (single `897ba5d` DISCOVER commit; harvest dated a day before the commit) | 1 | rust-port SKILL Phase 0/1 + loop_state.template; standard PROPOSED | applied |
| 2026-06-14 | rust-port | **Configuration class: `rust_target == dest_repo` (port a source directly INTO an existing dest app) is a common port-and-merge config the orchestrator didn't document — it assumed a separate port crate then merge into Y, so the merge step/ledger duplicated the port step/parity-ledger.** Operator hand-annotated the collapse in loop_state + merge-ledger. Fix: a Phase 0/1 + merge-ledger §Port-in-place note — the merge collapses to "landed-in-dest + dest-not-regressed", the merge-ledger tracks class+landing only, the parity gate doubles as the dest-regression gate, one git context / one commit per cycle. Removes duplicate bookkeeping, no gate relaxed. | loop_state.md l8-18 + merge-ledger.md header (operator-annotated target==dest collapse) | 1 | rust-port SKILL Phase 0/1 + references/merge-ledger.md | applied |
| 2026-06-14 | rust-port | **Gate-precision class (recurrence): the whole-source binary source-runnable gate is too coarse for a partly-runnable source — it false-halts DISCOVER on external-service paths that are map-onto-substrate-verified (never run differentially against the source).** Refined step 6 to classify units locally-differentiable (MUST be runnable) vs external-service (verified via the mapped substrate path); the hard NEEDS-HUMAN trigger is preserved for every genuine wall (toolchain dead; a locally-differentiable unit unrunnable; merge Y/substrate absent) — only the false halt is removed. Strengthen-by-precision, recurrence 2 of the v1.7.1 source-runnable lesson. Gate-adjacent ⇒ prose APPLIED + flagged in proposed-upgrades.md for owner awareness. | MiroFish partly-runnable: loop_state l32-34, research l13-22 (Zep/OASIS need creds → map-onto-substrate) | 2 | rust-port SKILL Phase 1 step 6 (gate-adjacent: applied + owner-flagged) | applied |
